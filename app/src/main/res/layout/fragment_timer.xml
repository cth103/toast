<TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/timerTable"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:stretchColumns="*"
    tools:context=".TimerActivity">

    <TableRow>
        <TextView
            android:layout_height="fill_parent"
            android:layout_width="0dp"
            android:gravity="center_vertical"
            android:id="@+id/on"
            android:textSize="10pt"
            android:text="Heating targets:"
            android:layout_span="2" />
    </TableRow>

    <TableRow android:paddingTop="10pt">
        <ListView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/rulesList"/>
    </TableRow>

    <TableRow android:paddingTop="10pt" android:layout_width="wrap_content">
        <Button android:text="Add target"
            android:layout_width="wrap_content"
            android:id="@+id/addRule"/>

    </TableRow>
</TableLayout>
